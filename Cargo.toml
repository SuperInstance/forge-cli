[package]
name = "forge"
version = "0.1.0"
edition = "2021"
description = "CLI tool for the ForgeFlux ecosystem"

[[bin]]
name = "forge"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4"] }
